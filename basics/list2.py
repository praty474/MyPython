list = [1, 2, 4, 3, 8, 5, 6]
list2 = [6, 10, 15, 20 ]

# list.sort()
# list.reverse()

list.extend(list2) #extend() function modifies the original 
#list and returns the same list with new elements added to the end of the list.

# list.sort(reverse = True)

print(list)